import React from 'react';
import {Modal, Button} from 'react-bootstrap';

export default class showVideoAI extends React.Component {

    constructor()
    {
        super()
        this.state = {
            show: false,
            setShow: false
        }

        this.handleClose = this.state.setShow
        this.handleShow = this.state.setShow
    }
    
       
       render() {
            return(
                <>
      
    </>
            )
       }
}